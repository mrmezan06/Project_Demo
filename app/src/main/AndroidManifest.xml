<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mezan.atmdemo3">

    <application
        android:allowBackup="true"
        android:icon="@drawable/atm"
        android:label="@string/app_name"
        android:roundIcon="@drawable/atm"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".ShowBalance" />
        <activity android:name=".HomePage" />
        <activity android:name=".Startup">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ForgetAdminPassword" />
        <activity android:name=".About" />
        <activity android:name=".LoadAll" />
        <activity android:name=".TransactionShow" />
        <activity android:name=".DeleteTHistory" />
        <activity android:name=".UserHistory"></activity>
    </application>

</manifest>